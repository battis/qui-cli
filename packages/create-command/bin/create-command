#!/usr/bin/env node

import { Init } from '@qui-cli/init';
import { Core } from '@qui-cli/core';
import path from 'node:path';

Init.configure({ template: path.resolve(import.meta.dirname, '../template') });
await Core.run();
